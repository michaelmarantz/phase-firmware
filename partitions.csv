# Phase — 4 MB flash, OTA-capable layout (edition00.2)
#
# nvs and phy_init keep the exact offset/size of the original single-app
# table, so Wi-Fi creds + render params survive the first serial reflash
# onto this layout. otadata + two 1.9375 MB app slots fill the 4 MB chip.
# App slots must start on 0x10000 boundaries.
#
# Slot size (0x1F0000 = 2 031 616 bytes) leaves plenty of headroom over
# the current ~1 MB binary — no need to trim WPA3/IPv6/enterprise-Wi-Fi
# to fit, unlike the 2 MB layout.
#
# Name,     Type, SubType, Offset,    Size
nvs,        data, nvs,     0x9000,    0x6000
phy_init,   data, phy,     0xf000,    0x1000
otadata,    data, ota,     0x10000,   0x2000
ota_0,      app,  ota_0,   0x20000,   0x1F0000
ota_1,      app,  ota_1,   0x210000,  0x1F0000
